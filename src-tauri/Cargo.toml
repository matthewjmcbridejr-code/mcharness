[package]
name = "marius_desktop_shell"
version = "0.1.0"
edition = "2021"
build = "build.rs"

[build-dependencies]
tauri-build = "2.6.2"

[dependencies]
tauri = { version = "2.6.2", default-features = false, features = ["wry"] }
